import { getData, storeData, removeLockNumberData, removePasswordData, deleteData } from "./storage"
import { screenHeight, screenWidth } from "./screenDimension"
import { colors } from "./colors"

export { getData, storeData, removeLockNumberData, removePasswordData, deleteData, screenHeight, screenWidth, colors }